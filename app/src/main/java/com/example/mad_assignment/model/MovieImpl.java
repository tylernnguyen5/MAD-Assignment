package com.example.mad_assignment.model;

public class MovieImpl extends AbstractMovie {
    public MovieImpl(int id, String title, int year, String filename) {
        super(id, title, year, filename);
    }
}
